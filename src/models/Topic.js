class Language {
  constructor({
    id,
    name,
    level,
    progress,
  }) {
    this.id = id;
    this.name = name;
    this.level = level;
    this.progress = progress;
  }
}

export default Language;
